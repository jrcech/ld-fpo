/ frozen_string_literal: true

- flash.each do |type, message|
  div class="mt-3 alert alert-#{bootstrap_flash(type)[:class]}"
    = fa_icon(bootstrap_flash(type)[:icon], fixed_width: true, text: message)
